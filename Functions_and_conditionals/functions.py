def mean(myList):
    mean_value = sum(myList) / len(myList)
    return mean_value

print(mean([1,2,3,4,5]))

# 1. If nothing is return then None returned automatically
def mean(myList):
    mean_value = sum(myList) / len(myList)
    
print(mean([1,2,3,4,5])) 